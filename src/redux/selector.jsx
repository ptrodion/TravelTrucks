export const selectListOfVehicles = (state) => state.vehicles.listOfVehicles;
export const selectIsLoading = (state) => state.vehicles.isLoading;
export const selectError = (state) => state.vehicles.error;
export const selectFilters = (state) => state.vehicles.filters;
export const selectPage = (state) => state.vehicles.page;
export const selectLimit = (state) => state.vehicles.limit;
export const selectTotal = (state) => state.vehicles.total;
